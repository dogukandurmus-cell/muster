import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'com.sirket.ziyaret',
  appName: 'Müşteri Ziyaret Programı',
  webDir: 'dist',
  bundledWebRuntime: false
};
export default config;
